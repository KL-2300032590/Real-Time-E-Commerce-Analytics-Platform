package com.example.analytics.repo;

import com.example.analytics.model.UserEventRecord;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

@Repository
public interface UserEventRecordRepository extends JpaRepository<UserEventRecord, Long> {

    long countByEventType(String eventType);

    @Query("select coalesce(sum(e.price), 0) from UserEventRecord e where e.eventType = 'PURCHASE'")
    Double getTotalRevenue();

    @Query("select count(distinct e.userId) from UserEventRecord e")
    Long getUniqueUsers();
}
